let str = "Meow";

console.log(str.charAt(0)); // Pulling a speicific char from a string
console.log(str.length); // length of the string
console.log(str.replace("ow", "aryan")); // Replace a sequence of chars
console.log(str.substring(0, 3)); // Pull out a sub string from a string
console.log(str.concat("Aryan")); // Add this string after the original one